# 変更履歴

[日本語](CHANGELOG.ja.md) · [English](CHANGELOG.md)

各リリースの主な変更点です。リリースと APK は
[リリースページ](https://github.com/game-de-it/Pyxel-WASM-Frontend/releases)
にあります。

## 0.1.1 — 2026-09-11

### 追加

**ゲームが必要とする Python モジュールのインストール機能。** ゲームによっては
Pyxel 以外のモジュール（物理演算の pymunk など）を使っています。pwf が同梱して
いるのは Python と標準ライブラリだけなので、そうしたゲームはモジュールを要求する
行で止まってしまい、こちらから渡す手段もありませんでした。

- ⚙ →**Python パッケージ**→**モジュールを管理**で、そのゲーム専用の画面が開き
  ます。1 行に 1 モジュールで、読み込むもの・調べて不足しているもの・この版で
  端末にすでにあるものが並びます。
- 調べる側は、ゲーム自身のソースを読んでどのモジュールを要求しているかを見つけ、
  そのうち実際に入っていないものを挙げます。ゲームによっては「あれば使う、無くて
  も動く」という要求の仕方をするものもあり、その名前も一覧に混じります。表示され
  たもの全部ではなく、**エラーに出た名前**を取得してください。
- モジュールのダウンロードは pwf 自身が行い、アプリの中からゲームへ渡します。
  ゲームを動かしているページがインターネットへ直接つなぐことはありません。
- モジュールは、ゲームに設定した Pyxel のバージョンごとに分けて保持します。Pyxel
  のバージョンが違うと、ゲームを動かす Python のバージョンも変わり、モジュールは
  自分が作られた Python のバージョンでしか読み込めないためです。管理画面には対象
  の Pyxel と Python のバージョンが表示され、その版の分がまだ無いモジュールには
  印が付きます。もう一度調べれば、その版に合うものを取得します。
- 一度取得したモジュールは端末に残ります。同じモジュールを使う別のゲームでは、
  再ダウンロードせずに有効にできます。

**ゲームが停止したときに、内容を読める画面。** これまでは、失敗したゲームは自分の
画面にエラーを出すだけで、携帯機では読めない大きさでした。読める大きさで表示する
ようにし、その画面から不足しているモジュールをそのまま調べられます。

### 修正

- 仮想コントローラ「自動」が設定どおりに動作するようになりました。仮想コントローラ
  を「OFF」にしたゲームを遊んだあと、別のゲームを「自動」で起動しても、画面に
  コントローラが出ないままでした。なお、ブラウザは実機のコントローラを「一度使われ
  るまで」認識しないため、「自動」では起動直後だけ画面にコントローラが出て、認識
  された時点で消えることがあります。

## 0.1.0 — 2026-08-31

最初のリリースです。

- Pyodide と Pyxel の wasm ランタイムをアプリに同梱し、仮想オリジンから配信。
  ネットワークもブラウザもサーバも要りません。
- ライブラリ。1 本ずつ・フォルダごと・URL からの追加に対応し、元ファイルを
  追跡するので、新しいビルドで上書きすればそのまま反映されます。
- assets フォルダは遅延で参照するため、数百 MB あっても起動は遅くなりません。
- セーブは実行間で保持され、zip に書き出せます。ファイルの代わりにブラウザの
  保存領域を使うゲームの分も含みます。
- ゲームだけでなくランチャー自体もコントローラーで操作できます。
- ゲームごとの Pyxel バージョン指定。2.x 系全体から必要に応じて取得します。
- ランタイムのネットワーク更新。検証付き・不可分・失敗時は自動で差し戻します。
