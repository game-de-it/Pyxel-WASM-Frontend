# 変更履歴

[日本語](CHANGELOG.ja.md) · [English](CHANGELOG.md)

各リリースの主な変更点です。リリースと APK は
[リリースページ](https://github.com/game-de-it/Pyxel-WASM-Frontend/releases)
にあります。

## 0.1.1 — 2026-09-11

### 追加

**ゲームが必要とする Python パッケージ。** 同梱の Pyodide は core 配布です。
インタプリタと標準ライブラリだけで、それ以外はありません。pymunk や numpy を
import するゲームはその行で死に、内側からはどうにもできませんでした。

- ⚙ →**Python パッケージ**→**モジュールを管理**で、そのゲーム専用の画面が
  開きます。1 行に 1 モジュールで、読み込むもの・調べて不足しているもの・
  この版で端末にすでにあるものが並びます。
- 調べる側はゲームの import を**ソースから**読みます（実行はしません。実行こそが
  失敗する行為です）。そのうえでインタプリタに「どれが見つからないか」を
  問い合わせます。ゲームが「あれば使う」だけの import も混じるので、
  **エラーに出た名前**を取得してください。
- wheel の入手元は Pyodide の配布物か PyPI です。取得はネイティブ側で行い、
  アプリ自身のオリジンから配信します。WebView が外部 URL を読まないことは
  変わりません。
- すでに端末にあるパッケージは、再ダウンロードせずにそのゲームで有効にできます。
- import で止まったゲームには、4 ピクセル文字のトレースバックではなく、読める
  大きさのエラー画面が出ます。そこから調べられます。

### 変更

- パッケージは**ゲームの Pyxel バージョンに追従**します。古い Pyxel は古い
  Python で動き、片方向けの wheel はもう片方では読み込めないため、インタプリタ
  ごとに別々に持ちます。管理画面には対象の Pyxel・Python・ABI が表示され、
  いま選んでいる版の分がまだ無いモジュールには印が付きます。
- 0.1.0 で取得した wheel は、初回起動時に同梱ランタイムの ABI へ移行します。
  それ以外の版向けではあり得ないためです。

### 修正

- 仮想コントローラ `自動` が、起動のたびに「実機パッドが本当にあるか」で判断
  するようになりました。1 つのページで全ゲームを動かすため、以前は状態が
  持ち越され、`OFF` のゲームを経由すると次の `自動` のゲームでもパッドが
  出ませんでした。なお、ブラウザは実機パッドを「一度使われるまで」見つけない
  ため、`自動` では起動直後だけ表示され、認識された時点で消えることがあります。
- 調べる側が `math` などインタプリタ組み込みのモジュールを挙げなくなりました。
  ファイルとして存在しないうえ、PyPI には同名の無関係なプロジェクトがあります。

## 0.1.0 — 2026-08-31

最初のリリースです。

- Pyodide と Pyxel の wasm ランタイムをアプリに同梱し、仮想オリジンから配信。
  ネットワークもブラウザもサーバも要りません。
- ライブラリ。1 本ずつ・フォルダごと・URL からの追加に対応し、元ファイルを
  追跡するので、新しいビルドで上書きすればそのまま反映されます。
- assets フォルダは遅延で参照するため、数百 MB あっても起動は遅くなりません。
- セーブは実行間で保持され、zip に書き出せます。ファイルの代わりにブラウザの
  保存領域を使うゲームの分も含みます。
- ゲームだけでなくランチャー自体もコントローラーで操作できます。
- ゲームごとの Pyxel バージョン指定。2.x 系全体から必要に応じて取得します。
- ランタイムのネットワーク更新。検証付き・不可分・失敗時は自動で差し戻します。
